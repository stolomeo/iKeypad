import styled from "styled-components";

export default function Header() {
  return <Title>iKeypad</Title>;
}

const Title = styled.h1`
  color: white;
  text-align: center;
  margin-top: 1rem;
  font-size: 70px;
  font-family: "Roboto Slab", serif;
`;
